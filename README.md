# deltazulu-ou.github.io

Webpage for DeltaZulu OÜ.

## Local development

```bash
bundle install
bundle exec jekyll serve
```

## Build and deploy

This repository builds and deploys with GitHub Actions (`.github/workflows/jekyll.yml`) instead of relying on the `github-pages` gem lockstep runtime.
